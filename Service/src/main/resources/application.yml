server:
  address: localhost
  port: 8001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/service_api?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Nodo7102@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true


logging.level:
  org.hibernate.SQL: debug